#Run_analysis function

This function takes two data.tables that should have the same format, calculates the mean and sd for each variable based on the row labels 
(which could be an animal, a species, an individual) and returns a final data.table with these values. 

The function was created as project assignment for the 
